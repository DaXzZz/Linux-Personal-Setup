# ======= 🌟 Starship Prompt Config 🌟 =======
# Theme: Cyberpunk + Minimal + Git + Time + Custom Symbols
# -------------------------------------------

# กำหนดรูปแบบ Prompt โดยใช้สีพื้นหลังแยกโซน
format = """
[](#8be9fd)\
$username\
[](bg:#50fa7b fg:#8be9fd)\
$directory\
[](fg:#50fa7b bg:#ffb86c)\
$git_branch\
$git_status\
[](fg:#ffb86c bg:#ff5555)\
$cmd_duration\
[](fg:#ff5555 bg:none)\
\n$character"""

# ========== 🔹 USERNAME (ชื่อผู้ใช้) ==========
[username]
style_user = "bold #282a36 bg:#8be9fd"  # กำหนดสีชื่อผู้ใช้
format = '[ $user ]($style)'  # รูปแบบการแสดงผล
show_always = true  # ให้แสดงชื่อผู้ใช้ตลอดเวลา

# ========== 📂 DIRECTORY (โฟลเดอร์ที่ใช้งาน) ==========
[directory]
style = "bold #282a36 bg:#50fa7b"  # สีของไดเรกทอรี
format = "[ $path ]($style)"  # รูปแบบการแสดงผล
truncation_length = 3  # ตัดไดเรกทอรีให้สั้นลง (3 โฟลเดอร์)
truncate_to_repo = false  # ไม่ตัดชื่อโฟลเดอร์เมื่ออยู่ใน Git Repo

# ========== 🌿 GIT BRANCH (สาขา Git) ==========
[git_branch]
style = "bold #282a36 bg:#ffb86c"  # สีของ Git Branch
format = "[  $branch ]($style)"  # ใช้ไอคอน Git

# ========== 🔄 GIT STATUS (สถานะ Git) ==========
[git_status]
style = "bold #282a36 bg:#ffb86c"
format = '([+$ahead]($style) )([-$behind]($style) )([~$modified]($style) )'

# ========== ⏳ COMMAND DURATION (ระยะเวลารันคำสั่ง) ==========
[cmd_duration]
min_time = 500  # แสดงเฉพาะถ้าใช้เวลานานกว่า 500ms
style = "bold #282a36 bg:#ff5555"  # สีพื้นหลัง
format = "[ ⏳ $duration ]($style)"  # แสดงผลแบบ "⏳ 2s"

# ========== ✨ PROMPT SYMBOL (เครื่องหมาย Prompt) ==========
[character]
success_symbol = "[➜](bold #50fa7b)"  # แสดงเป็น "➜" ถ้าคำสั่งสำเร็จ
error_symbol = "[✗](bold #ff5555)"  # แสดงเป็น "✗" ถ้าคำสั่งผิดพลาด

# ========== 🕐 TIME (เวลา) ==========
[time]
disabled = false  # เปิดการแสดงผล
format = "[🕒 $time]($style) "
style = "bold blue"
time_format = "%H:%M:%S"  # แสดงเวลาแบบ HH:MM:SS

# ========== ⚙️ CUSTOM SYMBOLS ==========
[shell]
style = "bold cyan"
format = "[  $indicator]($style) "  # แสดงไอคอน Terminal

